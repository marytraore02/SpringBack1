export interface Region{
    id_region: number;
    code_region: string;
    domaine_activite_region: string;
    langue_majoritaire: string;
    nom_region: string;
    superficie: string;
    pays_id:string;
}